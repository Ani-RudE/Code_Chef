#include <bits/stdc++.h>
using namespace std;

int main()
{
     int T;
     int size;
     cin >> T;

     while (T--)
     {
          // int min = INT_MIN;
          cin >> size;
          int arr[size];
          for (long long int i = 0; i < size; i++)
          {
               cin >> arr[i];
               cout<<arr[i]<<endl;
          }

          long long int k = 1;
     }

     return 0;
}